"""为已被清扫的 spill 产物提供模型可理解的稳定提示。

harness 靠"永不删除"绕过了这个问题,常驻服务不能。
"""

from __future__ import annotations

# 不能只说"已过保留期":产物也可能因总体积上限在当天就被按最旧优先删掉,
# 那时告诉模型"过期了"会让它据此推断产物年龄,而那个推断是错的。
EXPIRED_NOTICE = (
    "该 spill 产物已因保留期或容量上限被清理,无法取回。"
    "如需完整内容请重新执行产生它的原命令。"
)
